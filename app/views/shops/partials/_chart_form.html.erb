
<form action="/shop_analysis" method='get' id="chart_form">
	<center>
		<%= select_tag 'duration_id', options_from_collection_for_select(ShopChartDisplayGeneral.find(:all), :id, :name, @duration.id) %>
	  <span class="separator"></span>
	  <%= hidden_field_tag 'id', @shop.id %>
		<%= submit_tag "フォロワー数を表示", :class => "link_button"%>
	</center>
</form>

